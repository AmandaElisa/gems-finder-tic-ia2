# Development dependencies: tests and notebooks. Deliberately NOT in
# requirements.txt, which Streamlit Community Cloud installs on deploy.
-r requirements.txt

# Tests
pytest>=7.0

# Notebooks (notebook/*.ipynb). Without these, gems_finder_eda.ipynb cannot
# run on a fresh clone — it imports matplotlib and seaborn.
jupyterlab>=4.0
matplotlib>=3.8
seaborn>=0.13

# scikit-learn is not imported by any notebook yet, but the methodology's
# feature-selection and k-NN sections need it — see
# docs/specs/2026-09-01-recommender-methodology/spec.md §6.2 and §6.3.
scikit-learn>=1.4
